//RouteName returns the unique name or each route in our application
//First route is initialized with / as its the root route.
class RouteName {
  static const String splashScreen = "/", loginScreen = "/login_screen", homeScreen = "/home_screen";
}
